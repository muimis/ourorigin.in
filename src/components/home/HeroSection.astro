---
// HeroSection.astro — The Living Frame (Act I)
---

<section class="hero-frame" id="hero" aria-label="The Ancient Land">

  <!-- Immersive Imagery (The Life Beating) -->
  <div class="hero-image-wrapper" data-reveal="fade">
    <img src="/assets/images/gesture_hands.png" alt="The First Hands" class="hero-image" />
    <div class="hero-image-gradient"></div>
  </div>

  <!-- Archival Telemetry (Rotated Left) -->
  <div class="hero-telemetry-vertical" id="hero-telemetry" data-reveal="fade">
    <span class="telemetry-coord">A GLOBAL ETHIC</span>
    <span class="telemetry-sep">·</span>
    <span class="telemetry-soil">THE DIGNITY OF THE FIRST HANDS</span>
  </div>

  <!-- Monumental Primeval Truth -->
  <div class="hero-content">
    <span class="hero-ancient-tag" data-reveal="fade">OURORIGIN</span>
    
    <h1 class="hero-title" data-reveal>
      Wherever the origin<br/>
      philosophy exists.<br/>
      <span class="hero-title-accent">We are the conduit.</span>
    </h1>

    <p class="hero-subtitle" data-reveal>
      From the ancient rainforests of the Western Ghats to indigenous communities worldwide. We record the truth of the soil and honor the hands that gather its yields.
    </p>

    <!-- Dual Action Matrix -->
    <div class="hero-actions" data-reveal>
      <a href="#elevation-realms" class="btn-hero btn-hero--descend">
        <span class="btn-text">Explore The Harvest</span>
        <span class="btn-arrow" aria-hidden="true">↓</span>
      </a>
    </div>
  </div>

</section>

<script>
  import { gsap } from 'gsap';

  function initHero() {
    const tl = gsap.timeline({ delay: 0.1 });

    // Slow, breathing image reveal
    tl.fromTo('.hero-image',
      { scale: 1.05, opacity: 0 },
      { scale: 1, opacity: 0.85, duration: 3.5, ease: 'power2.out' }
    );

    tl.fromTo('#hero-telemetry',
      { opacity: 0, x: -16 },
      { opacity: 1, x: 0, duration: 2.0, ease: 'power3.out' },
      '-=3.0'
    );

    tl.fromTo('.hero-title',
      { opacity: 0, y: 32, filter: 'blur(8px)' },
      { opacity: 1, y: 0, filter: 'blur(0px)', duration: 2.2, ease: 'power2.out' },
      '-=2.5'
    );

    tl.fromTo('.hero-subtitle',
      { opacity: 0, y: 20 },
      { opacity: 1, y: 0, duration: 1.8, ease: 'power2.out' },
      '-=2.0'
    );

    tl.fromTo('.hero-actions',
      { opacity: 0, y: 16 },
      { opacity: 1, y: 0, duration: 1.5, ease: 'power2.out' },
      '-=1.5'
    );
  }

  initHero();
  document.addEventListener('astro:after-swap', initHero);
  document.addEventListener('astro:page-load', initHero);
</script>

<style>
  .hero-frame {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: clamp(6.5rem, 14vh, 10rem) clamp(4rem, 8vw, 10rem) clamp(4rem, 8vh, 6rem);
    overflow: hidden;
    background: transparent;
    z-index: 1;
  }

  /* Immersive Image */
  .hero-image-wrapper {
    position: absolute;
    top: 0;
    right: 0;
    bottom: 0;
    width: 65%;
    z-index: 0;
    overflow: hidden;
  }

  .hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center left;
    opacity: 0.85;
    mix-blend-mode: luminosity; /* Blends nicely with the warm loam background */
  }

  .hero-image-gradient {
    position: absolute;
    inset: 0;
    background: linear-gradient(to right, var(--color-forest-deep) 0%, rgba(18, 14, 11, 0.4) 40%, rgba(18, 14, 11, 0) 100%);
  }

  /* Vertical Telemetry */
  .hero-telemetry-vertical {
    position: absolute;
    left: clamp(1rem, 3vw, 2.5rem);
    top: 50%;
    transform: translateY(-50%) rotate(-90deg);
    transform-origin: center left;
    z-index: 2;
    display: flex;
    align-items: center;
    gap: 1rem;
    font-family: var(--font-evidence);
    font-size: var(--type-whisper);
    color: var(--color-cream-muted);
    letter-spacing: 0.2em;
    white-space: nowrap;
  }

  .telemetry-coord {
    color: var(--color-gold);
    font-weight: 500;
  }

  .telemetry-sep {
    color: var(--color-cream-whisper);
  }

  /* Hero Content Overlap */
  .hero-content {
    position: relative;
    z-index: 2;
    max-width: 900px;
    margin-left: clamp(2rem, 4vw, 6rem);
  }

  .hero-ancient-tag {
    display: inline-block;
    font-family: var(--font-evidence);
    font-size: var(--type-whisper);
    letter-spacing: 0.3em;
    color: var(--color-gold);
    margin-bottom: 1.5rem;
    text-transform: uppercase;
  }

  .hero-title {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(3rem, 7.5vw, 6.5rem); /* Massive typography */
    line-height: 1.05;
    font-weight: 400;
    color: var(--color-warm-cream);
    letter-spacing: -0.02em;
    text-wrap: balance;
    text-shadow: 0 10px 40px rgba(0, 0, 0, 0.5); /* Lift off the image */
  }

  .hero-title-accent {
    color: var(--color-gold-bright);
    font-style: italic;
    font-family: var(--font-narrative);
    font-weight: 300;
    letter-spacing: 0;
  }

  .hero-subtitle {
    margin: clamp(2rem, 4vh, 2.5rem) 0 0;
    font-family: var(--font-narrative);
    font-size: clamp(1.2rem, 2.2vw, 1.6rem); /* Larger, editorial size */
    line-height: 1.6;
    color: rgba(240, 234, 214, 0.85);
    max-width: 580px;
    font-weight: 300;
    text-shadow: 0 4px 12px rgba(0, 0, 0, 0.4);
  }

  /* Actions */
  .hero-actions {
    margin-top: clamp(2.5rem, 5vh, 4rem);
  }

  .btn-hero {
    display: inline-flex;
    align-items: center;
    gap: 1rem;
    padding: 1rem 2rem;
    font-family: var(--font-evidence);
    font-size: var(--type-evidence);
    letter-spacing: 0.15em;
    text-transform: uppercase;
    text-decoration: none;
    border: 1px solid var(--color-gold);
    color: var(--color-warm-cream);
    background: rgba(212, 139, 22, 0.05);
    border-radius: var(--radius);
    transition: all var(--duration-slow) var(--ease-organic);
    cursor: pointer;
    backdrop-filter: blur(4px);
  }

  .btn-hero:hover {
    background: rgba(212, 139, 22, 0.15);
    transform: translateY(-2px);
    box-shadow: 0 10px 30px rgba(212, 139, 22, 0.15);
  }

  .btn-arrow {
    transition: transform var(--duration-slow) var(--ease-organic);
  }

  .btn-hero:hover .btn-arrow {
    transform: translateY(4px);
  }

  @media (max-width: 1024px) {
    .hero-image-wrapper {
      width: 100%;
      opacity: 0.4;
    }
    .hero-image-gradient {
      background: linear-gradient(to top, var(--color-forest-deep) 0%, rgba(18, 14, 11, 0.6) 50%, rgba(18, 14, 11, 0.2) 100%);
    }
    .hero-content {
      margin-left: 0;
    }
    .hero-telemetry-vertical {
      display: none; /* Hide on smaller screens */
    }
  }
</style>
