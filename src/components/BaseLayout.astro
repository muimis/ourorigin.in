---
import '../styles/tokens.css';
import CommandCenter from './CommandCenter.astro';
import AllYieldsPanel from './AllYieldsPanel.astro';
import Ledger from './Ledger.astro';
import Footer from './Footer.astro';

export interface Props {
  title: string;
  description: string;
  canonicalPath?: string;
  ogTitle?: string;
  ogDescription?: string;
  ogImage?: string;
  bodyClass?: string;
  noIndex?: boolean;
  navVariant?: 'dark' | 'light';
  showFooter?: boolean;
}

const {
  title,
  description,
  canonicalPath = '',
  ogTitle,
  ogDescription,
  ogImage = 'https://ourorigin.in/og-image.jpg',
  bodyClass = '',
  noIndex = false,
  navVariant = 'light',
  showFooter = true
} = Astro.props;

const canonicalUrl = canonicalPath ? `https://ourorigin.in${canonicalPath}` : undefined;
const resolvedOgTitle = ogTitle || title;
const resolvedOgDescription = ogDescription || description;
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{title}</title>

    <meta name="description" content={description}>
    {canonicalUrl && <link rel="canonical" href={canonicalUrl}>}
    {noIndex && <meta name="robots" content="noindex, nofollow">}

    <!-- Favicon -->
    <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    <link rel="icon" href="/favicon.ico" sizes="32x32">

    <!-- Open Graph -->
    <meta property="og:type" content="website">
    <meta property="og:title" content={resolvedOgTitle}>
    <meta property="og:description" content={resolvedOgDescription}>
    {canonicalUrl && <meta property="og:url" content={canonicalUrl}>}
    <meta property="og:image" content={ogImage}>
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:site_name" content="OurOrigin">

    <!-- Twitter / X -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content={resolvedOgTitle}>
    <meta name="twitter:description" content={resolvedOgDescription}>
    <meta name="twitter:image" content={ogImage}>

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400..900&family=Cormorant+Garamond:wght@300;400;500;600&family=DM+Mono:ital,wght@0,300;0,400;0,500&family=Inter:wght@300;400;500&display=swap" rel="stylesheet">

    <slot name="head" />
</head>
<body class={bodyClass}>
    <!-- Unified global UI navigation and checkout system -->
    <CommandCenter variant={navVariant} />
    <AllYieldsPanel />
    <Ledger />

    <script>import '../scripts/reveal.js';</script>

    <slot />

    {showFooter && <Footer />}
</body>
</html>
